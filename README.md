## Network Monitoring

Network activity is the primary behavioral signal being evaluated against application workload.

The Raspberry Pi acts as the network monitoring device. It captures packets generated by the test machine and stores them in a PCAP file.

```text
Windows test machine
        |
        | Network traffic
        v
Raspberry Pi
        |
        | packet capture
        v
     capture.pcap
        |
        | TCP transfer
        v
Windows
        |
        v
   received.pcap
        |
        v
      TShark
        |
        v
  packet_data.csv
